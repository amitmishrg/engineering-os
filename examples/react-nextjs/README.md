# Example — React / Next.js

```bash
cd examples/react-nextjs
npm init -y
# add react, next to package.json
npx @amitmishrg/engineering-os init --scan
```

See `../scenarios/editor-toggle.json` for performance profile scenarios.
